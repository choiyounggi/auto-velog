{
  "name": "blog-loop",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin: harvest blog-worthy moments from sessions and auto-publish styled posts to Velog",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
